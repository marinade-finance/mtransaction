syntax = "proto3";

package validator;

// The MTransaction service definition.
service MTransaction {
  // Sends message to be streamed back
  rpc TxStream(TxMessageRequest) returns (stream TxMessage) {}
}

message TxMessageRequest {}

// The response message
message TxMessage {
  string data = 1;
}
